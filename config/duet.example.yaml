# ────────────────────────────────────────────────────────────────────────────
# Duet Configuration Example
# ────────────────────────────────────────────────────────────────────────────

# ──── Codex Configuration (Planning & Review) ────
codex:
  # Options: "codex", "echo"
  # - "codex": Use real Codex CLI (requires local authentication)
  # - "echo": Fallback mode for testing without API calls
  provider: "echo"  # Change to "codex" for production
  model: "gpt-4"
  # timeout: 300  # Optional: CLI timeout in seconds

# ──── Claude Code Configuration (Implementation) ────
claude:
  # Options: "claude-code", "echo"
  # - "claude-code": Use real Claude Code CLI (requires local authentication)
  # - "echo": Fallback mode for testing without API calls
  provider: "echo"  # Change to "claude-code" for production
  model: "claude-sonnet-4"
  # timeout: 600  # Optional: CLI timeout in seconds

# ──── Workflow Settings ────
workflow:
  max_iterations: 3
  require_human_approval: true
  auto_merge_on_approval: false

  # Guardrails
  max_consecutive_replans: 3  # Max replans before human approval
  # max_phase_runtime_seconds: 600  # Optional: max runtime per phase
  require_git_changes: true  # Fail IMPLEMENT if no changes detected
  use_feature_branches: true  # Create duet/<run-id> branches
  restore_branch_on_complete: true  # Restore original branch when done

# ──── Storage Settings ────
storage:
  workspace_root: "./"
  run_artifact_dir: "./runs"

# ──── Logging Settings ────
logging:
  enable_jsonl: false  # Set to true for structured logging
  jsonl_dir: "./logs"

# ────────────────────────────────────────────────────────────────────────────
# Production Configuration Example
# ────────────────────────────────────────────────────────────────────────────
# codex:
#   provider: "codex"
#   model: "gpt-4"
#   timeout: 300  # Optional: CLI timeout in seconds
#   # cli_path: "/custom/path/to/codex"  # Optional: custom CLI path
#
# claude:
#   provider: "claude-code"
#   model: "claude-sonnet-4"
#   timeout: 600  # Optional: CLI timeout in seconds
#   # cli_path: "/custom/path/to/claude"  # Optional: custom CLI path
#
# workflow:
#   max_iterations: 5
#   require_human_approval: true
#   auto_merge_on_approval: false
#
# storage:
#   workspace_root: "/path/to/your/project"
#   run_artifact_dir: "/path/to/artifacts"
#
# logging:
#   enable_jsonl: true
#   jsonl_dir: "/path/to/logs"
