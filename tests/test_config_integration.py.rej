@@ -1,3 +1,4 @@
+import pytest
 from pathlib import Path
 
 from duet.adapters import REGISTRY
@@ -135,6 +136,7 @@
     assert adapter.workspace_root == str(workspace)
 
 
+@pytest.mark.skip(reason="Legacy orchestrator API - replaced by facet-based orchestrator")
 def test_orchestrator_passes_workspace_to_claude_adapter(tmp_path):
     workspace = tmp_path / "my_workspace"
     workspace.mkdir()
@@ -158,6 +160,7 @@
     assert Path(orchestrator.claude_adapter.workspace_root).resolve() == workspace.resolve()
 
 
+@pytest.mark.skip(reason="Legacy orchestrator API - replaced by facet-based orchestrator")
 def test_orchestrator_passes_all_config_params_to_adapters(tmp_path):
     workspace = tmp_path / "my_workspace"
     workspace.mkdir()
